The A4 cover: a full-bleed photograph with a white title panel over its left half.

- Consumer provides `titleLines` (two to three uppercase lines: "Informe de / pertinencia / Filial Piura"), `program`, `date` ("Mayo 2024"), `author` (the unit that prepared it, without "Elaborado por:"), and a `photo` URL of the campus or the field.
- Without a photo the ground is `table-head`.
- Render the Folio and DocHeader above it, as on every other page.
